<Page x:Class="USCISTracker.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:USCISTracker.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:USCISTracker.ViewModels" mc:Ignorable="d">

    <Page.DataContext>
        <vm:MainPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveVisualStateGroup">
                <!--Narrow / Phone portrait-->
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for narrow view  -->
                        <!-- When narrow,hide detail column, master take entire place-->
                        <Setter Target="MasterColumn.Width" Value="*"></Setter>
                        <Setter Target="DetailColumn.Width" Value="0"></Setter>
                    </VisualState.Setters>
                </VisualState>
                
                <!--Normal/Phone Landscape/Tablet Portrait-->
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for normal view  -->
                        <!--Normal would show the Master and the detail-->
                        <Setter Target="MasterColumn.Width" Value="320"></Setter>
                        <Setter Target="DetailColumn.Width" Value="*"></Setter>
                    </VisualState.Setters>
                </VisualState>
                
                <!--Wide state/Wide screen PC/ Tablet lanscape -->
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for wide view  -->
                        <!--Wide would be the same as normal, but with expanded master a bit to take spaces-->
                        <Setter Target="MasterColumn.Width" Value="400"></Setter>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:PageHeader x:Name="pageHeader" RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True" Text="My USCIS Case">

            <AppBarButton x:Name="AddCaseAppbarButton" Icon="Add" Label="Add Case"></AppBarButton>
            <AppBarButton x:Name="SyncCaseStatusAppbarButton" Icon="Sync" Label="Check Status"></AppBarButton>
            <!--  secondary commands  -->
            <controls:PageHeader.SecondaryCommands>
                <AppBarButton Click="{x:Bind ViewModel.GotoSettings}" Label="Settings" />
                <AppBarButton Click="{x:Bind ViewModel.GotoPrivacy}" Label="Privacy" />
                <AppBarButton Click="{x:Bind ViewModel.GotoAbout}" Label="About" />
            </controls:PageHeader.SecondaryCommands>

        </controls:PageHeader>
        
        <!--MainGrid -->
        <Grid x:Name="RootGrid" 
              RelativePanel.AlignLeftWithPanel="True" 
              RelativePanel.Below="pageHeader">

            <!--Define the column definition-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="MasterColumn" Width="320"></ColumnDefinition>
                <ColumnDefinition x:Name="DetailColumn" Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
           
            
            <Grid x:Name="MasterGrid">
                <TextBlock>Master</TextBlock>
                <Button Click="Button_Click">Test me</Button>
            </Grid>

            <Grid x:Name="DetailGrid" Grid.Column="1" BorderBrush="Gray" BorderThickness="1,0,0,0">
                <TextBlock>Detail</TextBlock>
            </Grid>
            
        </Grid>
             

    </RelativePanel>
   
</Page>

